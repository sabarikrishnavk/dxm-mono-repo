import * as React from "react";

interface Props {
  children?: React.ReactNode;
  tenant?: String;
}

export const {{ pascalCase name }} = ({ children , tenant }: Props) => {
  return (
    <div>

      <h1>{{ name }} for {tenant}</h1>
      {children}
    </div>
  );
};
